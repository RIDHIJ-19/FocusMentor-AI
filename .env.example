# Copy this file to ".env" (same folder) and paste your key after the "=".
# Get a free key at https://console.groq.com -- no credit card required.
# The app reads this file directly; it is never opened by anyone else,
# never committed to git (see .gitignore), and never typed into the UI.
GROQ_API_KEY=
